# Application Modules

Create runtime modules under the component directories described in
`docs/ARCHITECTURE.md`. Keep provider, records, and audit code behind small
interfaces so the deterministic demo path can be tested without a camera or
network access.